import React from "react";

const CompanyIcon = ({ image }) => {
  return (
    <div className="company-icon">
      <img src={image} alt="company icon" />
    </div>
  );
};

export default CompanyIcon;
